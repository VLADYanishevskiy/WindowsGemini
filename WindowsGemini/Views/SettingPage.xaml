<Page
    x:Class="WindowsGemini.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsGemini.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:settingspageviewmodel="using:WindowsGemini.ViewModels.SettingsPageViewModel"
    xmlns:Converters="using:WindowsGemini.Models.ValueConverters" xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    MinHeight="500"
    Background="{ThemeResource HostBackdropBrush}">
    <Page.DataContext>
        <settingspageviewmodel:SettingsViewModel/>
    </Page.DataContext>
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary Source="/Assets/ThemeColors/ThemeDictionaryDark.xaml" x:Key="Dark"/>
                <ResourceDictionary Source="/Assets/ThemeColors/ThemeDictionaryLight.xaml" x:Key="Light"/>
            </ResourceDictionary.ThemeDictionaries>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/Styles/RadioButtonAccentColorsStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid >
        <Grid x:Name="TitleBarGrid" VerticalAlignment="Top" Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="WindowsGemini" Grid.Column="1" Foreground="{ThemeResource GeneralTxbForeground}" VerticalAlignment="Center" FontSize="12"/>
        </Grid>
        <StackPanel Padding="11,20,0,0">
            <StackPanel.Resources>
                <Converters:EnumBooleanConverter x:Key="enumBooleanConverter" />
                <Converters:ModeEnumBooleanConverter x:Key="modeEnumBooleanConverter"/>
            </StackPanel.Resources>
            <TextBlock Text="Settings" Foreground="{ThemeResource GeneralBlueWhiteButtonBackground}" Margin="10,10,0,0" FontSize="18"/>
            <ScrollViewer Margin="13,10,0,0" Height="450">
                <controls:WrapPanel  Orientation="Horizontal" >
                    <StackPanel x:Name="TopStackSettings" Orientation="Vertical">
                        <TextBlock Text="Language" Margin="0,12,0,0" FontSize="18" Foreground="{ThemeResource GeneralTxbForeground}"/>
                        <ComboBox Width="350" Height="32" BorderThickness="1" Margin="0,12,0,0" Foreground="{ThemeResource GeneralTxbForeground}">
                            <ComboBoxItem Content="English" FontSize="16"/>
                            <ComboBoxItem Content="Rus"/>
                        </ComboBox>
                        <TextBlock Text="Duplicate files deletion mode" Margin="0,20,0,0" FontSize="18" Foreground="{ThemeResource GeneralTxbForeground}"/>

                        <RadioButton GroupName="deleteMode" IsChecked="{Binding Path=DeleTionMode, Converter={StaticResource modeEnumBooleanConverter}, ConverterParameter=SendToTecycleBin , Mode=TwoWay}" FontSize="16">Send to Recycle Bin</RadioButton>
                        <RadioButton GroupName="deleteMode" IsChecked="{Binding Path=DeleTionMode, Converter={StaticResource modeEnumBooleanConverter}, ConverterParameter=PermanentDeletion , Mode=TwoWay}" FontSize="16">Permanent deletion</RadioButton>


                        <TextBlock Text="Mode" Margin="0,20,0,0" FontSize="18" Foreground="{ThemeResource GeneralTxbForeground}"/>

                        <RadioButton GroupName="themeMode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=Light , Mode=TwoWay}" FontSize="16" Foreground="{ThemeResource GeneralTxbForeground}">Light</RadioButton>
                        <RadioButton GroupName="themeMode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=Dark , Mode=TwoWay}" FontSize="16" Foreground="{ThemeResource GeneralTxbForeground}">Dark</RadioButton>
                        <RadioButton GroupName="themeMode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=UseDefault , Mode=TwoWay}" FontSize="16" Foreground="{ThemeResource GeneralTxbForeground}">Use system settings</RadioButton>

                        <TextBlock Text="Accent colors" Margin="0,20,0,0" FontSize="18" Foreground="{ThemeResource GeneralTxbForeground}"/>


                        <GridView ItemsSource="{Binding AccentColors}" SelectionMode="Single">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <RadioButton GroupName="AccentColors" 
                                             HorizontalAlignment="Left"
                                             Style="{StaticResource rbtnRectStyle}" 
                                             Background="{Binding}"
                                             BorderThickness="3" BorderBrush="White"
                                             />
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel  Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                    </StackPanel>

                    <StackPanel Height="450" HorizontalAlignment="Stretch">
                        <TextBlock Text="About" Foreground="{ThemeResource GeneralTxbForeground}" FontSize="18"/>

                        <TextBlock Foreground="{ThemeResource TxbPressPlusForeground}" FontSize="16" Margin="0,13,0,0">
                            <Run Text="SomeNameApp"/>
                            <LineBreak/>
                            <Run Text="Version "/>
                            <Run Text="4151512"/>
                            <LineBreak/>
                            <Run Text="Microsoft Research"/>
                        </TextBlock>


                        <TextBlock FontSize="16" Width="465" Padding="0" Margin="0 , 20 , 0 ,0 " Foreground="{ThemeResource GeneralTxbForeground}">
                            <Run Text="Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do"/>
                            <LineBreak/>
                            <Run Text="eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut"/>
                            <LineBreak/>
                            <Run Text="enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi"/>
                            <LineBreak/>
                            <Run Text="ut aliquip ex ea commodo consequat. Duis aute irure dolor in "/>
                            <LineBreak/>
                        </TextBlock>

                        <Button Background="Transparent" Padding="0" Content="Share application" FontSize="16" Margin="0,24,0,24" Foreground="{ThemeResource GeneralBlueWhiteButtonBackground}"/>
                        <Button Background="Transparent" Padding="0" Content="More apps" FontSize="16" Foreground="{ThemeResource GeneralBlueWhiteButtonBackground}"/>
                        <Button Background="Transparent" Padding="0" Content="Send feedback" FontSize="16" Margin="0,20,0,0" Foreground="{ThemeResource GeneralBlueWhiteButtonBackground}"/>

                    </StackPanel>

                </controls:WrapPanel>
            </ScrollViewer>
        </StackPanel>
        
    </Grid>
</Page>
