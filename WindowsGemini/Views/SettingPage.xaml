<Page
    x:Class="WindowsGemini.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsGemini.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:settingspageviewmodel="using:WindowsGemini.ViewModels.SettingsPageViewModel"
    xmlns:Converters="using:WindowsGemini.Models.ValueConverters"
    mc:Ignorable="d"
    Width="500"
    Height="500"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">
    <Page.DataContext>
        <settingspageviewmodel:SettingsViewModel/>
    </Page.DataContext>
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/Styles/RadioButtonAccentColorsStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid>
        <Grid x:Name="TitleBarGrid" VerticalAlignment="Top" Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="WindowsGemini" Grid.Column="1" VerticalAlignment="Center" FontSize="12"/>
        </Grid>
        <StackPanel Padding="11,20,0,0">
            <StackPanel.Resources>
                <Converters:EnumBooleanConverter x:Key="enumBooleanConverter" />
            </StackPanel.Resources>
            <ScrollViewer>
                <StackPanel Orientation="Vertical" Padding="13,18,0,0">
                    <TextBlock Text="Settings" Foreground="#0078d7" FontSize="18"/>
                    <TextBlock Text="Language" Margin="0,12,0,0" FontSize="18"/>
                    <ComboBox Width="350" Height="32" BorderThickness="1" Margin="0,12,0,0">
                        <ComboBoxItem Content="English" FontSize="16"/>
                        <ComboBoxItem Content="Rus"/>
                    </ComboBox>
                    <TextBlock Text="Duplicate files deletion mode" Margin="0,20,0,0" FontSize="18"/>
                    <!--<RadioButton GroupName="deleteMode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=SendToTecycleBin , Mode=TwoWay}" FontSize="16">Send to Recycle Bin</RadioButton>
                    <RadioButton GroupName="deleteMode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=PermanentDeletion , Mode=TwoWay}" FontSize="16">Permanent deletion</RadioButton>-->


                    <TextBlock Text="Mode" Margin="0,20,0,0" FontSize="18"/>

                    <RadioButton GroupName="mode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=Light , Mode=TwoWay}" FontSize="16">Light</RadioButton>
                    <RadioButton GroupName="mode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=Dark , Mode=TwoWay}" FontSize="16">Dark</RadioButton>
                    <RadioButton GroupName="mode" IsChecked="{Binding Path=ApplicationTheme, Converter={StaticResource enumBooleanConverter}, ConverterParameter=UseDefault , Mode=TwoWay}" FontSize="16">Use system settings</RadioButton>

                    <TextBlock Text="Accent colors" Margin="0,20,0,0" FontSize="18"/>


                    <GridView ItemsSource="{Binding AccentColors}" SelectionMode="Single">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <RadioButton GroupName="AccentColors" 
                                             HorizontalAlignment="Left"
                                             Style="{StaticResource rbtnRectStyle}" 
                                             Background="{Binding}"
                                             BorderThickness="3" BorderBrush="White"
                                             />
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel  Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>

                   
                </StackPanel>
                
            </ScrollViewer>
        </StackPanel>
        
    </Grid>
</Page>
